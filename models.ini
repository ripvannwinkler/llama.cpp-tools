version = 1

; =============================================================================
; Per-model config for llama-server router mode.
; Referenced by start-llama.ps1 via --models-preset.
; Precedence (highest wins): CLI args  >  [model-id] section  >  [*] global.
; Section name = model id = the flat folder name under D:\llama.cpp\models.
; ctx-size = max that loads in the ROUTER at q5_1 KV on the 32 GB 5090.
; NOTE (2026-07-25): KV cache switched from q8_0 -> q5_1 for VRAM headroom; the
; per-model VRAM figures in comments below predate this change and are stale
; until re-verified.
; =============================================================================

[*]
n-gpu-layers = 99          ; offload all layers to the 5090
flash-attn   = on          ; required for quantized V cache
ctx-size     = 8192        ; fallback context if a model has no override
metrics      = on          ; exposes /metrics (n_decode_total) — the tray's auto-unload
                           ; idle detection uses this counter instead of point-in-time
                           ; /slots polling, which can miss short requests between ticks

; 27B dense (stock Qwen3.6-27B, Unsloth UD-Q4_K_XL) — replaces the removed
; Fable-Fusion-711 MTP preset. Full trained ctx (262144) confirmed to fit at
; q4_0 KV via llama-bench -d probe (2026-07-28): 163840/196608/229376/262144
; all loaded clean on the 32GB 5090 (16.39GB weights, plenty of headroom).
; Bumped KV to q8_0 same day: still fits at 262144 (30.3GB used, ~2.3GB
; headroom left vs ~6.5GB at q4_0) with no speed cost — llama-bench pp512/
; tg128 came back within noise of q4_0 (pp 3544->3595 t/s, tg 71.3->73.6
; t/s), so q8_0 is pure upside (lower KV quant error) at the cost of
; headroom. Re-check this preset first if VRAM gets tight from other GPU
; load. No MTP self-draft layers in this gguf (checked via gguf-dump: no
; mtp/draft/nextn tensors) — do not set spec-type, it will exit on load.
; ub stays 1024 (matches the dense-model bench figure: 2048 only gained
; 1.3% pp).
[Qwen3.6-27B-UD-Q4_K_XL]
cache-reuse        = 256
ctx-size           = 262144
batch-size         = 2048
ubatch-size        = 1024
cache-type-k       = q8_0
cache-type-v       = q8_0
; Custom fixed chat template — see froggeric/Qwen-Fixed-Chat-Templates note on
; the 35B preset below; the template covers all Qwen 3.6 variants.
jinja              = on
chat-template-file = D:\llama.cpp\models\_templates\Qwen3-Fixed-Chat-Template.jinja

; 35B MoE (A3B active) — Unsloth UD-Q4_K_XL. Replaces the NVFP4 quant.
; Ships MTP self-draft layers (gguf-dump: qwen35moe.nextn_predict_layers = 1),
; so spec-type = draft-mtp is enabled here (unlike the old NVFP4, which had MTP
; stripped and exited on load if spec-type was set). Full trained ctx 262144.
; Vision-enabled via mmproj-F16.gguf. KV q8_0 (near-lossless). Full 262144 loads
; at q8_0 but leaves only ~850MB headroom (31914/32768 MiB); a KV-quant step down
; to q5_1 barely helped (~320MB — this MoE's KV is tiny, weights 22.85GB + MTP
; draft + compute buffers dominate), so instead ctx is stepped down the grid
; (262144 -> 196608, in 32768 probe increments) to keep q8_0 quality with more
; VRAM margin. ub 2048 per the MoE bench figure (+8.6% pp vs 1024).
; Custom fixed chat template (fixes tool-call rendering / agentic-loop bugs in
; the official Qwen 3.6 template): froggeric/Qwen-Fixed-Chat-Templates, stored
; shared under models/_templates/. Requires jinja = on.
[Qwen3.6-35B-A3B-UD-Q4_K_XL_MTP]
cache-reuse        = 256
ctx-size           = 196608
batch-size         = 2048
ubatch-size        = 2048
cache-type-k       = q8_0
cache-type-v       = q8_0
spec-type          = draft-mtp
model              = D:\llama.cpp\models\Qwen3.6-35B-A3B-UD-Q4_K_XL_MTP\Qwen3.6-35B-A3B-UD-Q4_K_XL.gguf
mmproj             = D:\llama.cpp\models\Qwen3.6-35B-A3B-UD-Q4_K_XL_MTP\mmproj-F16.gguf
jinja              = on
chat-template-file = D:\llama.cpp\models\_templates\Qwen3-Fixed-Chat-Template.jinja

; 26B MoE (A4B active) — trained ctx is 262144. Replaces the removed
; Gemma-4-31B-it-QAT dense preset.
; ub 2048: 21.8GB @262k figure is stale (measured at q8_0 KV) — re-verify at q5_1
[Gemma-4-26B-A4B-it-QAT]
cache-reuse  = 256
ctx-size     = 262144
batch-size   = 2048
ubatch-size  = 2048
cache-type-k = q5_1
cache-type-v = q5_1

; 8B vision — full context fits once KV is quantized
[Qwen3-VL-8B-Instruct (Uncensored)]
model        = D:\llama.cpp\models\Qwen3-VL-8B-Instruct\Qwen3-VL-8B-Instruct-Q4_K_M.gguf
mmproj       = D:\llama.cpp\models\Qwen3-VL-8B-Instruct\mmproj-Qwen3-VL-8B-Instruct-F16.gguf
ctx-size     = 65536
batch-size   = 2048
ubatch-size  = 1024
cache-type-k = q5_1
cache-type-v = q5_1

; 8B LITE — SAME file, fixed 8192 ctx for a small VRAM footprint while doing other
; GPU-heavy work. Request model "qwen3-vl-8b-lite".
[Qwen3-VL-8B-Instruct (Lite, Uncensored)]
model        = D:\llama.cpp\models\Qwen3-VL-8B-Instruct\Qwen3-VL-8B-Instruct-Q4_K_M.gguf
mmproj       = D:\llama.cpp\models\Qwen3-VL-8B-Instruct\mmproj-Qwen3-VL-8B-Instruct-F16.gguf
ctx-size     = 4096
batch-size   = 1024
ubatch-size  = 256
cache-type-k = q4_0
cache-type-v = q4_0
